# odin-recipes
Practice
1. Installed homebrew package installer
2. Learn basic terminal functions (UNIX)
3. Use Git clone to clone repositories and use basic git functions like git status, git log, git commit, add, git push